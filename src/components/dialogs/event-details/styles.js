import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    paddingTop: 25,
  },
  spacer: {
    paddingRight: 20,
  },
}));

export default useStyles;
