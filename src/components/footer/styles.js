import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  footer: {
    position: 'absolute',
    bottom: 15,
    right: 15,
  },
}));

export default useStyles;
